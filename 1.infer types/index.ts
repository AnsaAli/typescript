//infer types / implicit types

let username = 'Ansa';
//above username is a string, and if we try to assign
//username = 10 , will show error. ie, ts will automatically detect the types
// and then, it will show/ infer the error
